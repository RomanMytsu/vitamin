"use strict";
(self["webpackChunkgulp"] = self["webpackChunkgulp"] || []).push([["src_ts_pages_profile_profile-overview_ts"],{

/***/ "./src/ts/pages/profile/profile-overview.ts"
/*!**************************************************!*\
  !*** ./src/ts/pages/profile/profile-overview.ts ***!
  \**************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   initProfileUpdate: () => (/* binding */ initProfileUpdate)
/* harmony export */ });
/* harmony import */ var libphonenumber_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! libphonenumber-js */ "./node_modules/libphonenumber-js/min/exports/parsePhoneNumber.js");
/* harmony import */ var libphonenumber_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! libphonenumber-js */ "./node_modules/libphonenumber-js/min/exports/AsYouType.js");
/* harmony import */ var _api_firestore_utils__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../api/firestore-utils */ "./src/ts/api/firestore-utils.ts");
/* harmony import */ var _firebase_firebase__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../firebase/firebase */ "./src/ts/firebase/firebase.ts");
/* harmony import */ var _utils_validate_form_validate_field__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../../utils/validate-form/validate-field */ "./src/ts/utils/validate-form/validate-field.ts");
/* harmony import */ var _utils_toast_toast__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../../utils/toast/toast */ "./src/ts/utils/toast/toast.ts");





function initProfileUpdate() {
    const form = document.querySelector("#form-overview");
    const button = document.querySelector("#profile-overview-btn");
    if (!form || !button)
        return;
    const validator = (0,_utils_validate_form_validate_field__WEBPACK_IMPORTED_MODULE_4__.initProfileValidator)();
    if (!validator)
        return;
    button.disabled = true;
    button.classList.remove("active");
    const checkInputs = () => {
        const inputs = Array.from(form.querySelectorAll("input"));
        const hasValue = inputs.some((input) => input.value.trim() !== "");
        button.disabled = !hasValue;
        button.classList.toggle("active", hasValue);
    };
    form.querySelectorAll("input").forEach((input) => {
        input.addEventListener("input", checkInputs);
    });
    const phoneInput = form.querySelector("#phone");
    const phoneError = form.querySelector("#phone-error");
    if (phoneInput) {
        phoneInput.addEventListener("input", (e) => {
            const target = e.target;
            if (!(target instanceof HTMLInputElement))
                return;
            let value = target.value.replace(/[^\d+]/g, "");
            if (!value.startsWith("+"))
                value = "+" + value.replace(/\D/g, "");
            let digits = value.replace(/\D/g, "");
            if (digits.length > 13)
                digits = digits.slice(0, 13);
            value = "+" + digits;
            const formatter = new libphonenumber_js__WEBPACK_IMPORTED_MODULE_1__.AsYouType();
            target.value = formatter.input(value);
        });
    }
    form.addEventListener("submit", async (e) => {
        e.preventDefault();
        if (button.disabled)
            return;
        const isValid = await validator.revalidate();
        if (!isValid)
            return;
        const user = _firebase_firebase__WEBPACK_IMPORTED_MODULE_3__.auth.currentUser;
        if (!user)
            return;
        const formData = new FormData(form);
        let formattedPhone;
        if (phoneInput) {
            const phoneValue = phoneInput.value.trim();
            if (phoneValue) {
                const phoneNumber = (0,libphonenumber_js__WEBPACK_IMPORTED_MODULE_0__.parsePhoneNumber)(phoneValue);
                if (!phoneNumber || !phoneNumber.isValid()) {
                    if (phoneError)
                        phoneError.textContent = "Enter a valid phone number";
                    return;
                }
                else {
                    if (phoneError)
                        phoneError.textContent = "";
                }
                formattedPhone = phoneNumber.formatInternational();
            }
        }
        const data = {
            firstName: String(formData.get("firstName") ?? ""),
            lastName: String(formData.get("lastName") ?? ""),
            address1: String(formData.get("address1") ?? ""),
            address2: String(formData.get("address2") ?? ""),
            city: String(formData.get("city") ?? ""),
            state: String(formData.get("state") ?? ""),
            zip: String(formData.get("zip") ?? ""),
            email: String(formData.get("email") ?? ""),
        };
        if (formattedPhone)
            data.phone = formattedPhone;
        button.disabled = true;
        try {
            await (0,_api_firestore_utils__WEBPACK_IMPORTED_MODULE_2__.updateDocument)("users", user.uid, data);
            (0,_utils_toast_toast__WEBPACK_IMPORTED_MODULE_5__.showToastSuccess)("Changes successfully saved");
            form.reset();
        }
        catch (err) {
            const message = err instanceof Error
                ? err.message
                : "Failed to save changes. Please try again.";
            (0,_utils_toast_toast__WEBPACK_IMPORTED_MODULE_5__.showToastError)(message);
            button.disabled = false;
        }
        finally {
            button.classList.remove("active");
        }
    });
}


/***/ }

}]);
//# sourceMappingURL=src_ts_pages_profile_profile-overview_ts.main.min.js.map