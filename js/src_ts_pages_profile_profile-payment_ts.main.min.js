"use strict";
(self["webpackChunkgulp"] = self["webpackChunkgulp"] || []).push([["src_ts_pages_profile_profile-payment_ts"],{

/***/ "./src/ts/pages/profile/profile-payment.ts"
/*!*************************************************!*\
  !*** ./src/ts/pages/profile/profile-payment.ts ***!
  \*************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   initPaymentUpdate: () => (/* binding */ initPaymentUpdate)
/* harmony export */ });
/* harmony import */ var _api_firestore_utils__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../api/firestore-utils */ "./src/ts/api/firestore-utils.ts");
/* harmony import */ var _firebase_firebase__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../firebase/firebase */ "./src/ts/firebase/firebase.ts");
/* harmony import */ var _utils_validate_form_validate_field__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../utils/validate-form/validate-field */ "./src/ts/utils/validate-form/validate-field.ts");
/* harmony import */ var _utils_toast_toast__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../utils/toast/toast */ "./src/ts/utils/toast/toast.ts");




function initPaymentUpdate() {
    const form = document.querySelector("#profile-payment-form");
    const button = document.querySelector("#payment-btn");
    if (!form || !button)
        return;
    const validator = (0,_utils_validate_form_validate_field__WEBPACK_IMPORTED_MODULE_2__.paymentFormValidator)();
    if (!validator)
        return;
    button.disabled = true;
    button.classList.remove("active");
    const checkInputs = () => {
        const inputs = Array.from(form.querySelectorAll("input"));
        const hasValue = inputs.every((input) => input.value.trim() !== "");
        button.disabled = !hasValue;
        button.classList.toggle("active", hasValue);
    };
    const cardInput = form.querySelector("#payment-card");
    if (cardInput) {
        cardInput.addEventListener("input", () => {
            let value = cardInput.value.replace(/\D/g, "");
            if (value.length > 16)
                value = value.slice(0, 16);
            cardInput.value = value.match(/.{1,4}/g)?.join("-") || value;
            checkInputs();
        });
    }
    const expInput = form.querySelector("#payment-expiration");
    if (expInput) {
        expInput.addEventListener("input", () => {
            let value = expInput.value.replace(/\D/g, "");
            if (value.length > 4)
                value = value.slice(0, 4);
            if (value.length >= 3) {
                value = value.slice(0, 2) + "/" + value.slice(2);
            }
            expInput.value = value;
            checkInputs();
        });
    }
    const cvcInput = form.querySelector("#payment-cvc");
    if (cvcInput) {
        cvcInput.addEventListener("input", () => {
            let value = cvcInput.value.replace(/\D/g, "");
            if (value.length > 3)
                value = value.slice(0, 3);
            cvcInput.value = value;
            checkInputs();
        });
    }
    form.addEventListener("submit", async (e) => {
        e.preventDefault();
        if (button.disabled)
            return;
        const isValid = await validator.revalidate();
        if (!isValid)
            return;
        const user = _firebase_firebase__WEBPACK_IMPORTED_MODULE_1__.auth.currentUser;
        if (!user)
            return;
        const formData = new FormData(form);
        const data = {
            cardNumber: String(formData.get("payment-card") ?? "").replace(/\D/g, ""),
            expiration: String(formData.get("payment-expiration") ?? ""),
            cvc: String(formData.get("payment-cvc") ?? ""),
        };
        button.disabled = true;
        try {
            await (0,_api_firestore_utils__WEBPACK_IMPORTED_MODULE_0__.updateDocument)("users", user.uid, {
                cardNumber: data.cardNumber,
                expiration: data.expiration,
                cvc: data.cvc,
            });
            (0,_utils_toast_toast__WEBPACK_IMPORTED_MODULE_3__.showToastSuccess)("Payment method saved successfully");
        }
        catch (err) {
            const message = err instanceof Error
                ? err.message
                : "Failed to save payment method. Please try again.";
            (0,_utils_toast_toast__WEBPACK_IMPORTED_MODULE_3__.showToastError)(message);
            button.disabled = false;
        }
        finally {
            button.classList.remove("active");
        }
    });
}


/***/ }

}]);
//# sourceMappingURL=src_ts_pages_profile_profile-payment_ts.main.min.js.map